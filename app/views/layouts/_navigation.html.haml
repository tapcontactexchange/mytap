= link_to "ZAP App", root_path, :class => 'brand'
%ul.nav.pull-right
  - if signed_in?
    %li
      = link_to(current_user.full_name, '#')
    %li
      = link_to('Logout', logout_path, :method => 'delete')
  - else
    %li
      = link_to('Login', new_session_path)
