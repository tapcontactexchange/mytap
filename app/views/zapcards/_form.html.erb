<%= form_for(@zapcard) do |f| %>
  <% if @zapcard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zapcard.errors.count, "error") %> prohibited this zapcard from being saved:</h2>

      <ul>
      <% @zapcard.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
